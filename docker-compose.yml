services:
  postgres:
    container_name: api-refund
    image: "bitnami/postgresql:latest"
    ports:
      - "${POSTGRES_PORT}"
    environment:
      POSTGRES_USER: "${POSTGRES_USER}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      POSTGRES_DB: "POSTGRES_DB"